#include "core.h"

int main()
{

    open_app();

    main_menu();

    return 0; // probably won't even get here, but just in case
                // ***the program will probably shut down by exit(0) from the close_app function***
}
